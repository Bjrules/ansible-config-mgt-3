pipeline {
    agent any
     stages{
       stage('Build') {
        steps {
          script {
            sh 'echo "BUilders"'
        }
      }
    }
    stage('Cleanup') {
      steps {
          cleanWs()
        
      }
    }
    stage("Initial cleanup") {
          steps {
            dir("${WORKSPACE}") {
              deleteDir()
            }
          }
        }
    }
}
